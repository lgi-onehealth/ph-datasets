process {
    
    withName: 'PROCESS_METADATA' {
        ext.args = ""
        ext.prefix = ""
        publishDir = [
            path: { "${params.outdir}/metadata },
            mode: { "${params.mode}" },
            pattern = '*.{csv,yml}'
        ]
    }
    withName: 'FFQ' {
        ext.args = ""
        ext.prefix = ""
        publishDir = [
            path: { "${params.outdir}/${meta.id}" },
            mode: { "${params.mode}" },
            pattern = '*.{fastq.gz,yml}"
        ]
    }

}